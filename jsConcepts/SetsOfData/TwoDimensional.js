// create a 2D array (key-val pairs)

// empty arr

var items = []; // nothin'

// or one at a time

var person = new Array(); // or [];


// push shit to it

person.push({"name" : "Joe", "age" : "43"});
person.push({"name" : "Julian", "age" : "22"});

console.log(person);

// get specific values Array[indexPos]["nameOfValue"]
console.log( person[0]["name"] ); // Joe
console.log( person[1]["name"] ); // Julian
